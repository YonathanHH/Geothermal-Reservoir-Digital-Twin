{
  "name": "geothermal-digital-twin",
  "version": "0.1.0",
  "private": true,
  "description": "Physics-informed probabilistic modelling for geothermal resource assessment.",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter @geo/core build && pnpm --filter web build",
    "start": "pnpm --filter web start",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b --pretty",
    "verify": "tsx packages/bench/verify.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "fflate": "^0.8.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}