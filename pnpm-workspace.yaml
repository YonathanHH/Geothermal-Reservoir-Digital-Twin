packages:
  - 'packages/*'
  - 'apps/*'

# esbuild (pulled in by vitest and tsx) runs a postinstall to fetch its platform
# binary. pnpm 11 fails the install unless build scripts are explicitly reviewed.
onlyBuiltDependencies:
  - esbuild
